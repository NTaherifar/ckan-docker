apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ckan-helm.fullname" . }}-test-connection"
  labels:
    {{- include "ckan-helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ckan-helm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
